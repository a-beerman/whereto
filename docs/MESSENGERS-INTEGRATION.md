# Интеграция мессенджеров для заведений

## Зачем это нужно?

**Мессенджеры очень популярны в странах СНГ** для связи с заведениями:

- **Telegram**: Очень популярен в Молдове, России, Украине
- **WhatsApp**: Универсальный, работает везде
- **Viber**: Популярен в странах СНГ
- **Facebook Messenger**: Для международной аудитории

### Преимущества мессенджеров:

1. ✅ **Быстрая связь**: Прямой чат быстрее, чем телефонный звонок
2. ✅ **Удобство**: Можно отправить сообщение в любое время
3. ✅ **Контекст**: Можно отправить скриншот, ссылку, детали
4. ✅ **История**: Все сообщения сохраняются
5. ✅ **Популярность**: В СНГ мессенджеры часто предпочтительнее звонков

## Что реализовано

### 1. Расширенная структура socialMedia

```typescript
socialMedia?: {
  // Social networks
  facebook?: string;
  instagram?: string;
  twitter?: string;
  // Messengers (priority)
  telegram?: string;    // Telegram username or link (explicit only)
  whatsapp?: string;    // WhatsApp phone number or link (optional - auto-generated from phone)
  viber?: string;       // Viber phone number or link (optional - auto-generated from phone)
  messenger?: string;    // Facebook Messenger link (explicit only)
}
```

### 2. Автоматическая генерация ссылок из номера телефона

**Важно**: Если у заведения есть номер телефона, бот **автоматически создает ссылки** для:

- ✅ **WhatsApp**: `https://wa.me/PHONE` (из номера телефона)
- ✅ **Viber**: `viber://chat?number=+PHONE` (из номера телефона)

**Не требуют номера телефона** (только явное указание):

- ❌ **Telegram**: Требует username или ссылку (нельзя создать из номера)
- ❌ **Messenger**: Требует ссылку или username (нельзя создать из номера)

### 3. Поддержка в боте

Бот автоматически создает кнопки для всех доступных мессенджеров:

- 💬 Telegram (только если явно указан)
- 💚 WhatsApp (автоматически из телефона или явно указан)
- 💜 Viber (автоматически из телефона или явно указан)
- 💙 Messenger (только если явно указан)

### 3. Переопределение через VenueOverrides

Можно вручную добавить/изменить мессенджеры через `VenueOverrides`:

- `phoneOverride`
- `websiteOverride`
- `socialMediaOverride` (полная структура с мессенджерами)

## Как получить данные мессенджеров?

### ✅ Автоматически из номера телефона (WhatsApp, Viber)

**Если у заведения есть номер телефона** (из Google Places API), бот **автоматически создает ссылки** для:

- WhatsApp: `https://wa.me/PHONE`
- Viber: `viber://chat?number=+PHONE`

**Не нужно ничего добавлять вручную!** Просто синхронизируйте заведения с Google Places API, и если у них есть телефон, WhatsApp и Viber кнопки появятся автоматически.

### Вариант 1: Google Places API (базовые данные)

**Google Places API предоставляет**:

- ✅ `phone` (телефон) → **автоматически создает WhatsApp и Viber ссылки**
- ✅ `website` (сайт)

**Google Places API НЕ предоставляет**:

- ❌ Telegram username
- ❌ Facebook Messenger ссылки
- ❌ Прямые ссылки на мессенджеры

### Вариант 2: Ручное добавление через Overrides (для Telegram, Messenger)

Для мессенджеров, которые **нельзя создать из номера телефона** (Telegram, Messenger), можно добавить вручную через `VenueOverrides`:

```sql
UPDATE venue_overrides
SET social_media_override = '{
  "telegram": "https://t.me/restaurant_name",
  "messenger": "https://m.me/restaurant_name"
}'::jsonb
WHERE venue_id = 'venue-uuid';
```

**Примечание**: WhatsApp и Viber в `socialMediaOverride` имеют приоритет над автоматически сгенерированными ссылками. Это полезно, если у заведения есть специальная ссылка или номер отличается от основного телефона.

### Вариант 3: Парсинг сайта заведения (будущее)

Можно парсить сайт заведения для поиска ссылок на мессенджеры:

- Искать ссылки типа `t.me/...`, `wa.me/...`, `viber://...`
- Искать иконки мессенджеров на странице
- Парсить мета-теги или структурированные данные

**Пример реализации** (будущее):

```typescript
async function extractMessengersFromWebsite(website: string): Promise<SocialMedia> {
  // Fetch website HTML
  // Parse for messenger links
  // Return found links
}
```

### Вариант 4: Пользовательский ввод (будущее)

Позволить владельцам заведений самим добавлять свои мессенджеры через бота или веб-интерфейс.

## Логика приоритетов

### WhatsApp и Viber

1. **Если в `socialMedia.whatsapp` или `socialMedia.viber` есть явная ссылка** → используется она
2. **Если явной ссылки нет, но есть `phone`** → автоматически генерируется ссылка из номера телефона
3. **Если нет ни того, ни другого** → кнопка не показывается

**Пример**:

```typescript
// Вариант 1: Автоматическая генерация
venue = {
  phone: '+37360123456',
  socialMedia: {}, // или undefined
};
// → WhatsApp и Viber кнопки появятся автоматически

// Вариант 2: Явная ссылка (приоритет)
venue = {
  phone: '+37360123456',
  socialMedia: {
    whatsapp: 'https://wa.me/37360123456?text=Hello', // Специальная ссылка с текстом
  },
};
// → Используется явная ссылка (с текстом), а не автоматически сгенерированная

// Вариант 3: Нет телефона, нет ссылок
venue = {
  phone: undefined,
  socialMedia: {},
};
// → WhatsApp и Viber кнопки не появятся
```

### Telegram и Messenger

- **Только явное указание**: Требуют username или ссылку, нельзя создать из номера телефона
- **Если не указаны** → кнопки не показываются

## Форматы ссылок

### Telegram

- `https://t.me/username` - полная ссылка
- `@username` - короткий формат (автоматически конвертируется)
- `username` - без @ (автоматически конвертируется)

### WhatsApp

- `https://wa.me/37360123456` - полная ссылка
- `+37360123456` - номер телефона (автоматически конвертируется)
- `37360123456` - без + (автоматически конвертируется)

### Viber

- `viber://chat?number=+37360123456` - протокол Viber
- `+37360123456` - номер телефона (автоматически конвертируется)

### Facebook Messenger

- `https://m.me/username` - полная ссылка
- `https://www.facebook.com/messages/t/username` - альтернативный формат

## Примеры использования

### В боте

Когда пользователь нажимает "Забронировать" для непартнерского заведения:

**Пример 1: Заведение с телефоном (автоматические WhatsApp и Viber)**

```
📞 Свяжитесь с заведением напрямую

**Restaurant Name**
📍 Address

Доступные способы связи:
📞 +37360123456
🌐 https://restaurant.md
💬 WhatsApp, Viber

[Кнопки: Позвонить | Открыть сайт | WhatsApp | Viber | Google Maps]
```

**Пример 2: Заведение с телефоном + Telegram (явно указан)**

```
📞 Свяжитесь с заведением напрямую

**Restaurant Name**
📍 Address

Доступные способы связи:
📞 +37360123456
🌐 https://restaurant.md
💬 Telegram, WhatsApp, Viber

[Кнопки: Позвонить | Открыть сайт | Telegram | WhatsApp | Viber | Google Maps]
```

**Важно**: WhatsApp и Viber кнопки появляются автоматически, если есть номер телефона, даже если они не указаны в `socialMedia`!

### В API

```json
{
  "id": "venue-uuid",
  "name": "Restaurant Name",
  "phone": "+37360123456",
  "website": "https://restaurant.md",
  "socialMedia": {
    "telegram": "https://t.me/restaurant_name",
    "whatsapp": "+37360123456",
    "viber": "+37360123456",
    "instagram": "https://instagram.com/restaurant"
  }
}
```

## Рекомендации для MVP

### Phase 1 (текущий):

1. ✅ Структура данных готова
2. ✅ Бот показывает кнопки мессенджеров
3. ✅ **WhatsApp и Viber работают автоматически** из номера телефона (из Google Places API)
4. ⚠️ Telegram и Messenger добавляются вручную через Overrides (если нужно)

**Для большинства заведений достаточно просто синхронизировать данные из Google Places API** - WhatsApp и Viber кнопки появятся автоматически, если есть номер телефона!

### Phase 2 (будущее):

1. Парсинг сайтов заведений для автоматического извлечения Telegram/Messenger ссылок
2. Веб-интерфейс для владельцев заведений
3. Интеграция с Telegram Bot API для прямых чатов

## Вывод

**Да, добавление мессенджеров имеет смысл**, потому что:

1. ✅ **Популярность**: В СНГ мессенджеры часто предпочтительнее звонков
2. ✅ **Удобство**: Прямая связь через чат быстрее и удобнее
3. ✅ **Конкурентное преимущество**: Не все сервисы предоставляют мессенджеры
4. ✅ **Автоматизация**: WhatsApp и Viber работают автоматически из номера телефона
5. ✅ **Гибкость**: Можно переопределять через Overrides без изменения основной структуры

**Для MVP**:

- ✅ **WhatsApp и Viber работают автоматически** - просто синхронизируйте заведения с Google Places API
- ⚠️ Telegram и Messenger можно добавить вручную через Overrides (если нужно)
- 🚀 В будущем можно автоматизировать через парсинг сайтов
